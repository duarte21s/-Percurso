import { CenaEntrada } from "@/components/hero/CenaEntrada";

/** A abertura fica fora do grupo (site), que inclui nav, avisos e rodapé. */
export default function Home() {
  return <CenaEntrada />;
}
