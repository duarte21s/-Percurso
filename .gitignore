node_modules/
.next/
out/
build/
.render-tools/
renders/

.env
.env.local
.env*.local

# Notas de desenvolvimento com credencial de teste. Fica fora do repositório
# pelo mesmo motivo do .env.local: senha não se versiona, nem de conta de teste.
NOTAS-LOCAIS.md

next-env.d.ts
*.tsbuildinfo

.DS_Store
Thumbs.db

.vercel
.env*

# Skills instaladas por ferramenta (npx skills add). São dependências externas,
# reinstaláveis por comando — não versionamos, como node_modules.
.agents/
.claude/skills/
# Exceção: o apple-hig já está commitado e é a autoridade de design do projeto.
!.claude/skills/apple-hig/
# Trava do instalador. Descreve o que está em .agents/, que também não versionamos.
skills-lock.json
